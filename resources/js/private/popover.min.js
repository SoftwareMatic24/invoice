function Popover(){function t(){var t=event.target;if(null!=t){var o=t.getBoundingClientRect(),r=t.dataset.popover;let e=t.dataset.popoverDirection;e=e||"top";t=`
			<style>
				.popover {
					font-size: 1.25rem;
					font-weight: 400;
					line-height: 1.4;
					color: #fff;
					background-color: var(--clr-neutral-700);
					display: flex;
					align-items: center;
					justify-content: center;
					max-width: 20rem;
					padding: 0.7rem 1.6rem;
					border-radius: 1000vmax;
					z-index:9;
					pointer-events: none;
					user-select: none;
				}

				.popover-top {
					position: absolute;
					left:${o.x}px;
					top:${o.y}px;
					transform: translate(-30%, calc(-100% - 0.5rem));
				}

				.popover-left {
					position: absolute;
					left:${o.x}px;
					top:${o.y}px;
					transform: translate(-105%, 0%);
				}


			</style>
		<span class="popover popover-${e}">${r}</span>`;document.querySelector("body").insertAdjacentHTML("beforeend",t)}}function o(){document.querySelectorAll(".popover").forEach(e=>e.remove())}return{init:function(){document.querySelectorAll('[data-popover]:not([data-popover-event-added="true"])').forEach(e=>{e.setAttribute("data-popover-event-added",!0),e.addEventListener("mouseenter",t),e.addEventListener("mouseleave",o)})}}}let popover=Popover(),popoverElements=(popover.init(),document.querySelectorAll("[data-info]"));function showPopover(e){var t=e.target,o=window.getComputedStyle(t,":before");e.target===t&&0<=e.offsetX&&0<=e.offsetY&&e.offsetX<parseInt(o.width)&&(e.offsetY,parseInt(o.height))}popoverElements.forEach(e=>{e.addEventListener("mouseenter",function(){showPopover(event)})});